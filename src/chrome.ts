/** Shared chrome tokens for the JS-injected popup styles — the toolbar submenu flyout
 *  ({@link ./toolbar}) and the widget picker flower/grid ({@link ./widget}). One source of truth for
 *  the surface / border / shadow / hover / ink / sizes so they can't drift between the two blocks. */
export const CHROME_SURFACE = "#fff";
export const CHROME_BORDER = "1px solid rgba(0,0,0,.15)";
export const CHROME_SHADOW = "0 2px 8px rgba(0,0,0,.3)";
export const CHROME_HOVER = "#f4f4f4";
export const CHROME_INK = "#24292f";
export const CHROME_BTN_PX = 30; // square button box (px)
export const CHROME_GLYPH_PX = 18; // glyph inside a chrome button (px)
